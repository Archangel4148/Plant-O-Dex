import { View } from "react-native";
export default function community() {
    return (
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
        }}
      >
        
      </View>
    );
  }
  
  const styles = {
    
  }
  